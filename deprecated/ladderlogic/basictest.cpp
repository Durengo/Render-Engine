#include "basictest.h"


#include "gatesMemory/gatesHeaderLoader.h"


void basictest::runtest(){
/*
		using namespace lgates;
		NOT NOT1("NOT");
		NOT NOT2("NOT");
		AND AND1("AND");
		OR OR1("OR");
		int *input1 = new int;
		int *input2 = new int;
		int *input3 = new int;
		*input3 = 1;
		*input1 = 0;
		*input2 = 0;
		AND1.setNextPin(input1);
		AND1.setNextPin(input2);
		NOT2.setNextPin(input3);
		connector con(&AND1, &OR1);
		connector con2(&NOT2, &OR1);
		connector con3(&OR1, &NOT1);
		std::cout << "AND1: " << *AND1.getOutput() << "\n";
		std::cout << "NOT2: " << *NOT2.getOutput() << "\n";
		std::cout << "OR1: " << *OR1.getOutput() << "\n";
		std::cout << "NOT1: " << *NOT1.getOutput() << "\n";*/
}